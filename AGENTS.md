# Agent Instructions for the vime workspace

This workspace hosts the active AI-infra repos.

- Machine fallback: read `/home/aoshen/.claude/CLAUDE.md` and
  `/home/aoshen/AGENTS.md`.
- Workspace protocol: read `vllm-agent-infra/AGENTS.md` first, then
  `vllm-agent-infra/CORE.md`.
- vLLM source knowledge: read `vllm-knowledge/CLAUDE.md` and
  `vllm-knowledge/index.md`.

Prefer any repo-local `CLAUDE.md` / `AGENTS.md` inside the target repo first.
